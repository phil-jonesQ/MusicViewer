{
  "name": "musicviewerapi",
  "version": "1.0.0",
  "description": "A Simple API, when called with a input string, search the database and return music file contents based on the input string.",
  "main": "MusicViewerAPI.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon MusicViewerAPI.js --exec babel-node --"
  },
  "author": "Phil Jones",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "body-parser": "^1.18.3",
    "express": "^4.16.4"
  },
  "devDependencies": {
    "nodemon": "^1.18.9"
  }
}
